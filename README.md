# 시계부

하루 시간을 어디에 썼는지 기록하는 PWA 앱

## 설치 및 실행

```bash
npm install
npm run dev
```

## 배포 (GitHub Pages)

1. `.env.example`을 `.env`로 복사하고 API 키 입력
2. `npm run build`
3. `dist` 폴더를 GitHub Pages에 배포

## AI 총평 설정

`VITE_ANTHROPIC_API_KEY` 환경변수에 Anthropic API 키를 입력하세요.
